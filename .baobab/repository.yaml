schema_version: 1
repository:
  lifecycle: experimental
capabilities:
  - engine
  - rust
  - container
environment:
  baobab_dev:
    required: true
    profile: full
package_managers:
  rust: cargo
artifacts:
  container: true
security:
  sast_provider: fallback
